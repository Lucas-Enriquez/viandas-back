spring.datasource.url=${TEST_DB_URL:jdbc:postgresql://localhost:5432/viandas_test}
spring.datasource.username=${TEST_DB_USERNAME:postgres}
spring.datasource.password=${TEST_DB_PASSWORD:postgres}

spring.jpa.hibernate.ddl-auto=update

viandas.jwt.secret=test-secret-change-me-test-secret-change-me
viandas.bootstrap.key=test-bootstrap-key
viandas.public-base-url=caseritas://
viandas.google.client-id=
