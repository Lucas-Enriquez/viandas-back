# Perfil de desarrollo local.
# Activa el BootstrapController (/internal/bootstrap/cook).
# Para usar en IntelliJ: agregar -Dspring.profiles.active=dev en VM options,
# o setear SPRING_PROFILES_ACTIVE=dev en las variables de entorno del run config.
